import { EMPLOYEE_UPDATE } from '../actions/types'

const INITIAL_STATE = {};

export default (state = INITIAL_STATE, action ) => {
  switch (expression) {
    case expression:

      break;
    default:

  }
};
